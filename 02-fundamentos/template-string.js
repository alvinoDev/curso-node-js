const name = 'Superman';
const identity = 'Clark Kent';

const superHero = 'La identidad de ' + name + ' es ' + identity + '.';

const template = `La identidad de ${name} es ${identity}.`;

console.log(superHero);
console.log(template);


const html = `
<h1>Hello</h1>
<p>World!</p>
`;

console.log(html);
